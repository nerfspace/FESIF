# eBay Listing Monitor – Environment Variables
# Copy this file to .env and fill in the values

# Redis connection
REDIS_HOST=127.0.0.1
REDIS_PORT=6379

# SQLite database path
DB_PATH=./fesif.db

# Poller settings (seconds)
POLL_INTERVAL_MIN=5
POLL_INTERVAL_MAX=15

# eBay search keyword(s) – comma-separated list; blank = all categories
# e.g. EBAY_SEARCH_KEYWORD=iphone,gpu,macbook pro
EBAY_SEARCH_KEYWORD=

# eBay API credentials (get from https://developer.ebay.com)
# When set, the poller uses the official Browse API instead of HTML scraping.
# If not set, the app falls back to HTML scraping automatically.
EBAY_APP_ID=
EBAY_CERT_ID=

# Daily eBay API call budget (default: 5000 – the standard free-tier limit).
# 30% is reserved for polling (Browse API) and 70% for sold-price lookups (Finding API).
# The poller auto-calculates its interval from this budget and the number of keywords.
EBAY_DAILY_API_LIMIT=5000

# Minimum deal score returned by the API
MIN_DEAL_SCORE=80

# API port
API_PORT=3000

# Optional: comma-separated proxy list  (e.g. http://user:pass@host:port)
# PROXIES=http://proxy1:8080,http://proxy2:8080
